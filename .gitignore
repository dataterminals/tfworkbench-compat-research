# Python (compat tooling)
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# UE4SS / mod artifacts that should never live in the repo
*.pak
*.utoc
*.ucas
*.sig
*.dll
*.log

# Built/patched binaries live here locally but are never committed (see dist/README.md)
dist/*.dll

# Large downloaded UE4SS build zips kept for local bisecting (track by hash in data/, not by blob)
builds/
*.zip
